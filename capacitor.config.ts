import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.skinify.app',
  appName: 'Skinify',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
